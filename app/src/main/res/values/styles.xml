<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="colorPrimary">@color/colorMainRed</item>
        <!--<item name="colorPrimaryDark">@color/primary_dark</item>-->
        <!--<item name="colorAccent">?attr/colorPrimary</item>-->
        <!--<item name="colorButtonNormal">@color/primary</item>-->
        <!--<item name="android:homeAsUpIndicator">@drawable/colorMainRed</item>-->
        <item name="colorControlNormal">@color/colorWarmGray</item>
        <!--I disable it becouse didn't work ovveride  (needed for radio button in location resycleView)-->
        <item name="colorControlActivated">@color/colorMainRed</item>
        <item name="colorControlHighlight">@color/colorWarmGray</item>

        <!-- disable multitouch in app, for cases when user can click 2 and more buttons -->
        <item name="android:splitMotionEvents">false</item>

        <!--<item name="android:dropDownListViewStyle">@style/SpinnerDropdownStyle</item>-->
        <item name="android:windowBackground">@color/colorLightGray</item>
    </style>

    <style name="Wrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="Match">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="MatchWidth">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="toolbar_icon" parent="Wrap">
        <item name="android:layout_gravity">center_vertical|left</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
</resources>
